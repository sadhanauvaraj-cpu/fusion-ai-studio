### START / END (internal)

- Backend `type`: `START`, `END`
- Not user-created. Used to define pipeline structure.
- IMPORTANT: `END` is hidden in the diagram and should be treated as implicit termination.
